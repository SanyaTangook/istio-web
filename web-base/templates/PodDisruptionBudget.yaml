apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-pdb
spec:
  minAvailable: {{ .Values.infrastructure.Replicas.min }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}
